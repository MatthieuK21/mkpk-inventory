[build]
builder = "NIXPACKS"

[deploy]
# Migrations SQL avant démarrage (sans GitHub Actions)
preDeployCommand = ["npm", "run", "db:migrate"]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
